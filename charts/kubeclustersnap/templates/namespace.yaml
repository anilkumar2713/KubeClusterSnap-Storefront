{{- if .Values.createNamespace }}
apiVersion: v1
kind: Namespace
metadata:
  name: {{ .Release.Namespace }}
  labels:
    app.kubernetes.io/name: {{ include "kubeclustersnap.name" . | default "kubeclustersnap" }}
    app.kubernetes.io/managed-by: Helm
{{- end }}
